---
title: Header 01
description: Responsive navigation header with logo and menu links.
component: true
categories: [header]
---

<TemplateDisplay name="header-01" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx codebase add header-01
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the registry dependencies.</Step>

```bash
npx codebase add button navigation-menu sheet
```

<Step>Copy the template files into your project.</Step>

The CLI writes each file to its `target` path (e.g. `app/...`, `components/...`, `config/...`). Replace the import paths to match your project setup.

</Steps>

</TabsContent>

</CodeTabs>

## Categories

header

## Registry Dependencies

- `button`
- `navigation-menu`
- `sheet`
