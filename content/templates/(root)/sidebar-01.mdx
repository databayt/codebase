---
title: Sidebar 01
description: Collapsible sidebar with multi-level navigation and search.
component: true
categories: [sidebar]
---

<TemplateDisplay name="sidebar-01" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx codebase add sidebar-01
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the registry dependencies.</Step>

```bash
npx codebase add sidebar button input tooltip dropdown-menu separator avatar collapsible
```

<Step>Copy the template files into your project.</Step>

The CLI writes each file to its `target` path (e.g. `app/...`, `components/...`, `config/...`). Replace the import paths to match your project setup.

</Steps>

</TabsContent>

</CodeTabs>

## Categories

sidebar

## Registry Dependencies

- `sidebar`
- `button`
- `input`
- `tooltip`
- `dropdown-menu`
- `separator`
- `avatar`
- `collapsible`
