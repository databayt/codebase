---
title: Dashboard 01
description: A dashboard with sidebar, charts and data table.
component: true
categories: [dashboard]
---

<TemplateDisplay name="dashboard-01" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx codebase add dashboard-01
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the registry dependencies.</Step>

```bash
npx codebase add button card input label select separator sidebar breadcrumb dropdown-menu avatar badge table chart
```

<Step>Copy the template files into your project.</Step>

The CLI writes each file to its `target` path (e.g. `app/...`, `components/...`, `config/...`). Replace the import paths to match your project setup.

</Steps>

</TabsContent>

</CodeTabs>

## Categories

dashboard

## Registry Dependencies

- `button`
- `card`
- `input`
- `label`
- `select`
- `separator`
- `sidebar`
- `breadcrumb`
- `dropdown-menu`
- `avatar`
- `badge`
- `table`
- `chart`
