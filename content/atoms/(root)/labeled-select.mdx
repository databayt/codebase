---
title: Labeled Select
description: A select dropdown with an associated label.
component: true
---

<ComponentPreview name="labeled-select" className="mb-4">
  <LabeledSelect
    label="Category"
    options={[
      { value: "billing", label: "Billing" },
      { value: "support", label: "Support" },
      { value: "account", label: "Account" },
    ]}
    defaultValue="billing"
  />
</ComponentPreview>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx codebase add labeled-select
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="labeled-select" title="components/atom/labeled-select.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { LabeledSelect } from "@/components/atom/labeled-select"
```

```tsx
<LabeledSelect
  label="Category"
  options={[
    { value: "billing", label: "Billing" },
    { value: "support", label: "Support" },
  ]}
  defaultValue="billing"
/>
```
